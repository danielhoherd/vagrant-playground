---
- name: Provision Vagrant vm
  hosts: all
  gather_facts: no
  become: yes

  tasks:
    - name: Install some packages we want
      apt:
        name: "{{item}}"
        state: present
      with_items:
        - curl
        - jq
