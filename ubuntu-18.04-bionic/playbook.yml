---
- name: Provision Vagrant VM
  hosts: all
  gather_facts: no
  become: yes

  tasks:
    - name: Install packages
      apt:
        name: "{{ item }}"
        state: present
      with_items:
        - curl
        - jq
        - wget
